#include<stdio.h>
#include<string.h>
int main()
{
    char length[]="viraj";
    int total;
    
 //   printf("Enter a string");
  //  scanf("%s",&length);
    total=strlen(length);
    printf("string %s length %d\n",length,total);
    return 0;
}